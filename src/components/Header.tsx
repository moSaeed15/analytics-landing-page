import React from 'react';
import Navbar from './Navbar';

const Header = () => {
  return (
    <div className="bg-dark-blue text-secondary">
      <Navbar />
    </div>
  );
};

export default Header;
